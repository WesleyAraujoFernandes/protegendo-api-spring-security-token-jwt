# URL de conexão com timezone atualizado para compatibilidade
spring.datasource.url=jdbc:mysql://localhost:3306/security?createDatabaseIfNotExist=true&serverTimezone=America/Sao_Paulo&useSSL=false&allowPublicKeyRetrieval=true

# Credenciais de acesso
spring.datasource.username=root
spring.datasource.password=X0nga$655365

# Configurações do Hibernate para Spring Boot 3.5
spring.jpa.hibernate.ddl-auto=update

# Exibição de logs SQL formatados no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate Dialect for MySQL 8
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect